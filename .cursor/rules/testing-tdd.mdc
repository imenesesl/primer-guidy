---
description: Mandatory testing with TDD — unit tests with React Testing Library / Vitest, E2E with Playwright, NestJS server tests
globs: "**/*.ts,**/*.tsx,**/*.test.ts,**/*.test.tsx,**/*.spec.ts"
alwaysApply: true
---

# Testing — Mandatory TDD

ALL code MUST have tests. No tests = not finished. Follow TDD strictly.

## TDD Workflow

1. **RED** — Write the test FIRST. It must fail.
2. **GREEN** — Write the minimum code to make it pass.
3. **REFACTOR** — Clean up without breaking tests.

NEVER write implementation without its test first. The test dictates the contract.

## Layer 1: Unit Tests — React Testing Library + Vitest (Web)

Every component, hook, utility, and service MUST have its `.test.tsx` or `.test.ts` file.

### File Structure

```
ComponentName/
  ComponentName.tsx
  ComponentName.module.scss
  ComponentName.types.ts
  ComponentName.utils.ts
  ComponentName.test.tsx      ← MANDATORY TEST
  index.ts
```

```
hooks/
  useAuth.ts
  useAuth.test.ts             ← MANDATORY TEST
```

```
utils/
  date.utils.ts
  date.utils.test.ts          ← MANDATORY TEST
```

### Testing Principles

- Test **user behavior**, not implementation details
- Use `screen`, `render`, `userEvent` — NEVER access `wrapper.instance()`
- Query priority: `getByRole` > `getByLabelText` > `getByText` > `getByTestId`
- `getByTestId` is a LAST RESORT — prefer accessible queries

### Example

```tsx
// Button.test.tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { Button } from './Button'

describe('Button', () => {
  it('calls onClick when clicked', async () => {
    const handleClick = vi.fn()
    render(<Button label="Save" variant="primary" onClick={handleClick} />)

    await userEvent.click(screen.getByRole('button', { name: 'Save' }))

    expect(handleClick).toHaveBeenCalledOnce()
  })

  it('renders as disabled when disabled prop is true', () => {
    render(<Button label="Save" variant="primary" onClick={vi.fn()} disabled />)

    expect(screen.getByRole('button', { name: 'Save' })).toBeDisabled()
  })
})
```

## Layer 2: Unit Tests — NestJS Servers + Shared Libs (Vitest)

Every controller, service, guard, adapter, and factory in NestJS servers and shared libs MUST have its `.test.ts` colocated next to the source file.

### File Structure

```
apps/<name>-server/src/
  modules/<feature>/
    <feature>.controller.ts
    <feature>.controller.test.ts  ← MANDATORY TEST
    <feature>.service.ts
    <feature>.service.test.ts     ← MANDATORY TEST
```

```
libs/llm-services/src/
  factory.ts
  factory.test.ts                 ← MANDATORY TEST
  adapters/
    anthropic.adapter.ts
    anthropic.adapter.test.ts     ← MANDATORY TEST
```

```
libs/nest-shared/src/
  guards/
    api-key.guard.ts
    api-key.guard.test.ts         ← MANDATORY TEST
  env/
    load-env.ts
    load-env.test.ts              ← MANDATORY TEST
```

### Testing Principles (NestJS)

- **Manual injection with mocks** — do NOT use NestJS testing module. Create the service with `new Service(mockDep as never)` passing mocks via constructor
- **vi.mock for external modules** — `vi.mock('dotenv', ...)`, `vi.mock('node:fs', ...)`
- **vi.stubGlobal for fetch** — `vi.stubGlobal('fetch', mockFetch)` for HTTP services
- **process.env for configuration** — manipulate directly in `beforeEach`, clean up with `delete process.env['KEY']`
- **No React Testing Library** — these are pure TypeScript services
- **vi.clearAllMocks() in beforeEach** — every test starts clean
- Tests MUST be independent — no order dependencies or shared mutable state

### Example — Service with LLM mock

```typescript
// generation.service.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import type { ILlmProvider, CompletionResult } from '@primer-guidy/llm-services'
import { GenerationService } from './generation.service'

const mockComplete = vi.fn<() => Promise<CompletionResult>>()
const mockProvider: ILlmProvider = { complete: mockComplete }

describe('GenerationService', () => {
  let service: GenerationService

  beforeEach(() => {
    vi.clearAllMocks()
    service = new GenerationService(mockProvider)
  })

  it('calls llm.complete with system prompt and user message', async () => {
    mockComplete.mockResolvedValueOnce({
      content: 'generated content',
      model: 'llama-3.1',
      tokensUsed: 42,
    })

    await service.generate('explain closures', 'JavaScript')

    expect(mockComplete).toHaveBeenCalledOnce()
    const [messages] = mockComplete.mock.calls[0]
    expect(messages[0].role).toBe('system')
    expect(messages[1].role).toBe('user')
  })
})
```

### Example — Guard with process.env

```typescript
// api-key.guard.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { UnauthorizedException } from '@nestjs/common'
import { ApiKeyGuard } from './api-key.guard'

const createMockContext = (headers: Record<string, string | undefined> = {}) => ({
  switchToHttp: () => ({ getRequest: () => ({ headers }) }),
})

describe('ApiKeyGuard', () => {
  const guard = new ApiKeyGuard()

  beforeEach(() => {
    vi.clearAllMocks()
    delete process.env['BRAIN_API_KEY']
  })

  it('allows request when X-API-Key matches', () => {
    process.env['BRAIN_API_KEY'] = 'secret'
    const ctx = createMockContext({ 'x-api-key': 'secret' })
    expect(guard.canActivate(ctx as never)).toBe(true)
  })

  it('throws when key is missing', () => {
    process.env['BRAIN_API_KEY'] = 'secret'
    const ctx = createMockContext({})
    expect(() => guard.canActivate(ctx as never)).toThrow(UnauthorizedException)
  })
})
```

### Example — HTTP client with vi.stubGlobal('fetch')

```typescript
// brain-client.service.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { HttpException } from '@nestjs/common'
import { BrainClientService } from './brain-client.service'

const mockFetch = vi.fn()

describe('BrainClientService', () => {
  beforeEach(() => {
    vi.clearAllMocks()
    process.env['BRAIN_BASE_URL'] = 'http://test:3011'
    process.env['BRAIN_API_KEY'] = 'test-key'
    vi.stubGlobal('fetch', mockFetch)
  })

  it('calls brain API with correct headers', async () => {
    mockFetch.mockResolvedValueOnce({
      ok: true,
      json: () => Promise.resolve({ content: 'ok', model: 'llama', tokensUsed: 1 }),
    })

    const service = new BrainClientService()
    await service.generate('prompt', 'ctx')

    expect(mockFetch).toHaveBeenCalledWith(
      'http://test:3011/api/generate',
      expect.objectContaining({
        headers: expect.objectContaining({ 'X-API-Key': 'test-key' }),
      }),
    )
  })

  it('throws HttpException on network error', async () => {
    mockFetch.mockRejectedValueOnce(new Error('ECONNREFUSED'))
    const service = new BrainClientService()
    await expect(service.generate('p', 'c')).rejects.toThrow(HttpException)
  })
})
```

### Vitest Config for Servers

Servers using vitest directly (without Nest test utilities) MUST have a `vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    globals: true,
    include: ['src/**/*.test.ts'],
  },
})
```

Server `tsconfig.json` files MUST exclude tests from the build: `"exclude": ["node_modules", "dist", "**/*.test.ts"]`

## Layer 3: E2E Tests — Playwright

Playwright validates complete user flows. They live in `e2e/` at the root of each app.

### Shared E2E Infrastructure — `@primer-guidy/e2e-helpers`

ALL emulator helpers, constants, and utilities live in `libs/e2e-helpers/`. NEVER duplicate E2E infrastructure in each app.

```typescript
// Shared helpers available from @primer-guidy/e2e-helpers
import {
  clearEmulators, createAuthUser, seedFirestoreProfile,  // Emulator management
  getOobCodeForEmail, buildEmailLinkUrl,                  // Magic link helpers
  signInOnPage,                                           // Auth helpers
} from '@primer-guidy/e2e-helpers'
```

The shared bash script `scripts/run-e2e.sh` handles emulator lifecycle. Apps call it via `"test:e2e": "bash ../../scripts/run-e2e.sh"`.

### Structure per App

```
apps/<name>-web/
  e2e/
    e2e.constants.ts          ← App-specific ONLY (E2E_USER)
    global-setup.ts           ← Imports from @primer-guidy/e2e-helpers
    global-teardown.ts        ← Imports from @primer-guidy/e2e-helpers
    playwright.config.ts      ← App-specific port and settings
    flows/
      feature.spec.ts         ← App-specific test specs
```

### What to Test with Playwright

- Route navigation
- Complete forms (submit, validation, feedback)
- Critical end-to-end user flows
- Error states and UI edge cases

### Example

```typescript
// e2e/flows/home.spec.ts
import { test, expect } from '@playwright/test'
import { signInOnPage } from '@primer-guidy/e2e-helpers'
import { E2E_USER } from '../e2e.constants'

test('displays home after auth', async ({ page }) => {
  await page.goto('/')
  await signInOnPage(page, E2E_USER)
  await expect(page.getByRole('heading', { level: 1 })).toBeVisible()
})
```

## Layer 4: Firebase Security Rules — `@firebase/rules-unit-testing`

Every rule in `firestore.rules` and `database.rules.json` MUST have a corresponding test in `tests/rules/`.

### Structure

```
tests/rules/
  vitest.config.ts             ← Config (environment: node, 30s timeout)
  firestore.test.ts            ← Tests for each match block in firestore.rules
  database.test.ts             ← Tests for each node in database.rules.json
```

### Execution

```bash
pnpm run test:rules
```

Starts emulators (Firestore + RTDB) automatically and runs the tests.

### What to Test

Each `describe` block MUST cover at minimum:
- Roles with access (owner, authenticated)
- Roles without access (non-owner, unauthenticated)
- Granular operations (read, create, update, delete) when rules differ by operation

### Example

```typescript
import { initializeTestEnvironment, assertSucceeds, assertFails } from '@firebase/rules-unit-testing'
import { doc, getDoc, setDoc } from 'firebase/firestore'

describe('users/{uid}', () => {
  it('owner can read own profile', async () => {
    const db = testEnv.authenticatedContext('teacher-1').firestore()
    await assertSucceeds(getDoc(doc(db, 'users/teacher-1')))
  })

  it('non-owner cannot read another profile', async () => {
    const db = testEnv.authenticatedContext('other').firestore()
    await assertFails(getDoc(doc(db, 'users/teacher-1')))
  })
})
```

## Mandatory Rules

- NEVER merge/PR without tests
- Minimum coverage: 80% per component/module
- Every bug fix MUST include a test that reproduces the bug BEFORE the fix
- Tests MUST be independent — no order dependencies or shared mutable state
- Every rule in `firestore.rules` or `database.rules.json` MUST have tests in `tests/rules/` — no test = rule not deployed
- Every NestJS service, controller, guard, and adapter MUST have its `.test.ts` — no test = not finished
- NestJS server tests use manual injection with mocks, NOT NestJS testing module
- Shared libs (`libs/llm-services`, `libs/nest-shared`) MUST have tests for their entire public API
