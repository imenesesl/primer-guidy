{
  "level": "node",
  "path": "studentCredentials/{identificationNumber}",
  "description": "Student credentials in Realtime Database — key is identification number for O(1) lookup",
  "schema": {
    "password": {
      "type": "string",
      "required": true,
      "description": "argon2id hashed password — hashing performed server-side in Cloud Functions"
    },
    "uid": {
      "type": "string",
      "required": true,
      "description": "Firebase Auth UID — created via Cloud Functions, links to Firestore students collection"
    }
  }
}
