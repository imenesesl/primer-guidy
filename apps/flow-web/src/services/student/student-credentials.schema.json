{
  "level": "node",
  "path": "student-credentials/{identificationNumber}",
  "description": "Student credentials in Realtime Database — key is identification number for O(1) lookup",
  "schema": {
    "password": {
      "type": "string",
      "required": true,
      "description": "SHA-256 hashed password"
    },
    "uid": {
      "type": "string",
      "required": true,
      "description": "Firebase Anonymous Auth UID — links to Firestore students collection"
    }
  }
}
