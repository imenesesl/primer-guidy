{
  "level": "realtime-database",
  "paths": {
    "codes/{code}": {
      "description": "Maps a 10-digit invite code to the user who owns it. Server-only write via generateInviteCode Cloud Function.",
      "schema": {
        "uid": {
          "type": "string",
          "required": true,
          "description": "Firebase Auth UID of the code owner"
        }
      }
    },
    "userCodes/{uid}": {
      "description": "Maps a user UID to their invite code. Server-only write via generateInviteCode Cloud Function. Client read allowed for owner.",
      "type": "string",
      "default": null
    }
  }
}
