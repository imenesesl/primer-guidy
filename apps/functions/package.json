{
  "name": "@primer-guidy/functions",
  "private": true,
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "argon2": "^0.43.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  }
}
